[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Single-page app: route everything through index.html so deep links still work.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
